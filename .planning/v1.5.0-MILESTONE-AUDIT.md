---
milestone: v1.5.0
milestone_name: Project-Scale Extract Design
audited: 2026-07-23
status: tech_debt
scores:
  requirements: 36/36
  phases: 11/11
  integration: PASS
  flows: 35/35
nyquist:
  compliant_phases: 11
  partial_phases: 0
  missing_phases: 0
  overall: COMPLIANT
gaps:
  requirements: []   # none unsatisfied — all 36 verified GOAL MET at their defined scope
  integration:
    - id: INT-P2-RUNTIME
      severity: warning   # design-intentional per phase plan; confirm against milestone intent
      req_ids: [INV-01, DISC-01, GRAPH-01, QUEUE-01, DEPCTX-01]
      finding: >
        Phase-2 deterministic primitives (inventory.mjs, discovery.mjs, graph-validation.mjs,
        queue-semantics.mjs, schedulability.mjs) are pure-function libraries with ZERO runtime
        consumers. main.mjs uses an LLM subsystem-decomposer + seedExtractQueue instead. The
        phase-2 plan (02-PLAN.md) explicitly scopes these as characterized contract primitives,
        not runtime integration targets — but the milestone GOAL promises "deterministic
        whole-project discovery." If runtime determinism was a hard requirement, this is a
        blocker; if contract characterization was intended, it is acceptable as-is.
      evidence: "design-budget.mjs et al. import nothing from P2; runtime path = main.mjs:1155 (subsystem-decomposer) + extract-scope.mjs seedExtractQueue; P2 exercised only by tests/e2e-matrix.test.mjs:151-203 + per-module tests"
    - id: INT-MIGRATION-RESUME
      severity: warning   # design-intentional
      req_ids: [CONTRACT-01]
      finding: >
        migrateLegacyState / validateMigrationBoundary (migration.mjs:163) are exported and
        unit-tested but never invoked by the resume path. A true v1.4.5 state file --resume will
        hit resume-invalid-state rather than auto-migrating. By design (migration is an
        external-tooling contract) — confirm auto-migration-on-resume was not expected.
      evidence: "main.mjs:101 calls loadPipelineStateWithRecovery + validatePipelineState but not migrateLegacyState; covered by compatibility-regression.test.mjs:205+"
  flows: []   # 35/35 E2E matrix rows have backing tests
tech_debt:
  - phase: docs-ledger
    items:
      - "REQUIREMENTS.md status ledger is STALE: ~20 requirements still marked '[ ] Pending' (e.g. CONTRACT-01, all Phase 8/9/11 reqs) despite all being implemented, validated, and UAT-verified GOAL MET. Inline sub-agents updated STATE.md, not the requirement checkboxes. Reconcile the traceability table to Complete."
      - "SUMMARY.md MISSING for Phase 9 and Phase 10 (recovered/late phases); gsd-tools completed_phases heuristic under-counts (9 vs true 11) as a result."
      - "No SUMMARY.md carries a 'requirements-completed' frontmatter field (all phases) — the 3-source cross-reference's source C is empty everywhere."
      - "gsd-tools state.complete-phase mis-counted (8/11) during Phase 11; STATE.md was hand-corrected to 11/11."
  - phase: 01
    items:
      - "validateMigrationBoundary mutates state (child._durable=true) despite a 'Pure: …without performing any writes' comment — internal accumulator pattern; align comment or return new state."
  - phase: 10
    items:
      - "designBudgetGate counts 1 call per gate invocation, not actual intra-gate agent calls (conservative — under-counts retries/escalations within a gate)."
      - "Two parallel budget systems coexist: Phase-5 global retryState + Phase-10 designBudget. Neither ceiling approached in normal runs; unify if budgets become binding."
  - phase: 05
    items:
      - "Token budgets uncharacterized by default (Infinity) — only the call ceiling is enforced in production; token characterization needs runtime measurement."
  - phase: 02
    items:
      - "Cycle-policy HITL checkpoint (flagged in roadmap for explicit user confirmation) was resolved autonomously as a configurable policy-map classification (defers final cycle policy to config). Confirm acceptable."
  - phase: release
    items:
      - "Milestone NOT yet tagged/released — marketplace pin still points at the shipped v1.4.5 baseline. ENGINE_VERSION=1.4.5 in dist is the intentional state-format compat tag, not drift."
---

# Milestone v1.5.0 — Audit Report

**Audited:** 2026-07-23 · **Status: `tech_debt`** (all requirements met, no critical blockers, accumulated debt + one scope-confirmation item to review)

## Verdict

The milestone **achieved its definition of done**: all **36/36 requirements** verified GOAL MET
(per-phase UAT VERIFICATION.md), **11/11 phases** complete, **Nyquist COMPLIANT** across the board,
cross-phase integration **PASS**, and **35/35 E2E matrix rows** have backing tests. The full suite is
**1458 passing / 0 failing**, build drift-free.

Status is `tech_debt` rather than `passed` because of accumulated documentation-hygiene debt and **one
scope-confirmation item** (INT-P2-RUNTIME) that could change the verdict depending on intent.

## Requirements Coverage (3-source cross-reference)

Sources: (A) REQUIREMENTS.md traceability table · (B) per-phase VERIFICATION.md · (C) SUMMARY.md `requirements-completed` frontmatter.

| Source | Finding |
|--------|---------|
| (A) REQUIREMENTS.md traceability | 36/36 mapped; ~20 checkboxes STALE (`[ ]` though done) |
| (B) Phase VERIFICATION.md | 11/11 phases verdict **GOAL MET** (all requirements passed at defined scope) |
| (C) SUMMARY `requirements-completed` | **ABSENT** for all phases (field never written) |

**Result:** 36/36 satisfied (manual verification confirms: implemented + tested + UAT GOAL MET + integration wired). **0 orphaned** (every REQ-ID is covered by its phase VERIFICATION). The stale (A) checkboxes and absent (C) field are documentation debt, not requirement gaps.

## Phase Verification

| Phase | VERIFICATION | VALIDATION (Nyquist) |
|-------|--------------|----------------------|
| 1–11 | GOAL MET (all) | nyquist_compliant: true (all 11) |

## Integration Check — PASS

Cross-phase wiring verified at every seam (cited `file:line` by the integration checker). Highlights:
- P1 reducers → P4 leaf (`extract-slice-entry.mjs:3,63`); P1 digest → P8 checkpoint → P11 `verifyArtifactDigest` (full write→snapshot→recover→verify chain).
- P3 leaf spawned by P4 `Workflow()` (`main.mjs:1333`) with direct-call fallback.
- P5 primitives → main extract-loop + P6 status projection; P6 status-truth shared by handoff + status mode.
- P8 `checkpointDesign` at 19 gates; P9 `deriveDesignReadiness` blocks `designReady=true` on any degradation.
- **P10 `designBudgetGate` is LIVE** — 12 call-sites + defn in main.mjs (the UAT-fixed item); P10 `design-budget` consumes P5 `budget-admission`.
- P11 transient-retry + digest verification wired into the shared agent core.

## Open Items Requiring a Decision

1. **INT-P2-RUNTIME (most important):** the Phase-2 *deterministic* scheduling primitives are pure libraries with **no runtime consumers** — the live extract path uses LLM decomposition. The phase plan scopes them as contract-characterization libraries, but the milestone goal says *"deterministic whole-project discovery."* **Confirm whether runtime determinism was a hard requirement.** If yes → insert a closure phase to wire P2 into the runtime; if no (contract characterization) → accept as-is.
2. **INT-MIGRATION-RESUME:** v1.4.5 state isn't auto-migrated on `--resume` (design-intentional external-tooling contract). Confirm acceptable.

## Tech Debt (non-blocking)

See YAML `tech_debt` above. Headline items: reconcile the stale REQUIREMENTS.md ledger; backfill SUMMARY.md for P9/P10 + `requirements-completed` frontmatter; align the `validateMigrationBoundary` "Pure" comment; consider unifying the two budget systems; characterize token budgets; confirm the autonomous cycle-policy resolution.

## Next Step

Resolve INT-P2-RUNTIME (user decision), then either `/gsd-complete-milestone 1.5.0` (accept debt) or insert a closure phase for any item you want addressed first.
