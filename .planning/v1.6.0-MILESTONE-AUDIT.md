---
milestone: v1.6.0
milestone_name: Design-Extract Determination
audited: 2026-07-24
status: tech_debt
scores:
  requirements: 16/16
  phases: 8/8
  integration: PASS
  flows: 11/11
gaps:
  requirements: []   # none unsatisfied — all 16 verified GOAL MET
  integration:
    - id: INT-W1
      severity: warning
      req_ids: [REMOVED-01, INVALIDATE-01]
      finding: "`onSliceRemoved` (main.mjs:3842-3847) calls invalidatePersistenceEvidence + applyLifecycleEvent(exclude) but NOT markStaleForSlice — unlike invalidateSliceChain (extract-slice.mjs:330). Works today (persistence invalidation forces re-publish → re-derive coverage), but the synthesis-staleness signal is implicit/asymmetric."
    - id: INT-W2
      severity: warning
      req_ids: [UPSERT-01, MATCH-01]
      finding: "Auto-update (main.mjs:1457-1515) + root-last commit (2022-2045) update only status/updatedAt; the registry entry's files/anchorPath/ownershipScopeDigest stay at the promotion-time snapshot. Sticky reuse keeps working, but if the ENTIRE original file set renames across two auto-updates, the next findFeature could degrade to weak-only → block."
  flows: []   # 11/11 E2E matrix rows have backing tests
tech_debt:
  - phase: docs-ledger
    items:
      - "REQUIREMENTS.md ledger is STALE: all 16 requirements still `[ ]` / traceability `Pending` despite every one being implemented, validated, and UAT-verified GOAL MET. (Inline sub-agents updated STATE/VERIFICATION, not the requirement checkboxes.)"
      - "SUMMARY.md MISSING for all 8 phases (12-19)."
      - "gsd-tools completed_phases heuristic under-counts (reports 0/8 vs actual 8)."
  - phase: 17
    items:
      - "INT-W1: onSliceRemoved should call markStaleForSlice for symmetry with invalidateSliceChain."
  - phase: 18
    items:
      - "INT-W2: auto-update should refresh the registry entry's files/anchor/digest from the current revision (so rename-degradation over multiple updates can't occur)."
      - "Adoption write-order (writeIdentity before writeRegistry, plan specifies reverse) — self-healing on retry; orphaned .identity.json re-derived."
  - phase: testing
    items:
      - "Several E2E tests assert pure-function chains / source-regex rather than a true multi-run `main()` invocation — brittle under refactor. Recommend ONE integration test calling main() twice (create → remove-a-slice / rename-all-originals) asserting synthesis staleness + registry freshness."
---

# Milestone v1.6.0 — Audit Report

**Audited:** 2026-07-24 · **Status: `tech_debt`** (all requirements met, no critical blockers, integration PASS; accumulated doc debt + 2 fragile-but-functional warnings)

## Verdict

The milestone **achieved its definition of done**: **16/16 requirements** verified GOAL MET (per-phase UAT VERIFICATION.md), **8/8 phases** complete, cross-phase integration **PASS**, **11/11 E2E matrix** scenarios backed by tests. Full suite **2431 passing / 0 failing**, build drift-free (383/382 names). The integration checker explicitly confirmed **no implemented-but-never-wired functions** (the v1.5.0 P10-DBUDGET failure mode is absent — the full update chain `resolveUpsertMode → reconcileSlices → runChangeDetection → invalidateSliceChain/onSliceRemoved → extract` is wired in main.mjs).

Status is `tech_debt` (not `passed`) due to accumulated documentation-hygiene debt and two non-blocking fragile seams (INT-W1/W2).

## Requirements Coverage (3-source cross-reference)

| Source | Finding |
|--------|---------|
| (A) REQUIREMENTS.md traceability | 16/16 mapped; **all checkboxes STALE** (`[ ]` / `Pending`) |
| (B) Phase VERIFICATION.md | **8/8 phases verdict GOAL MET** (all requirements passed) |
| (C) SUMMARY `requirements-completed` | **ABSENT** (no SUMMARY.md in any v1.6.0 phase) |

**Result:** 16/16 satisfied (manual verification: implemented + 2431 tests + integration PASS + 1 defect caught/fixed during P17 validation). **0 orphaned**, **0 unsatisfied**. Stale (A) checkboxes and absent (C) SUMMARYs are documentation debt, not requirement gaps.

## Integration Check — PASS (2 warnings)

All 7 critical seams WIRED end-to-end (file:line cited by the integration checker):
- **Update flow** (the core): `resolveUpsertMode → loadPipelineStateWithRecovery → reconcileSlices → runChangeDetection → invalidateSliceChain/onSliceRemoved → fall-through to extraction` (main.mjs:1399→1519).
- **Fresh-new flow**: preflight → findFeature(miss) → collision guard → promote(NEW) → writeIdentity → writeRegistry.
- Registry lookup wired pre-promotion (reuse/new/blocked); `recoverRegistry` at startup before Gate X0; `--confirm`/locator; `--adopt`/auto-scan root-qualified; root-last readiness commit.
- **11/11 E2E matrix** scenarios have backing tests.

**Warnings (non-blocking, fragile-but-functional):**
- **INT-W1** — `onSliceRemoved` doesn't call `markStaleForSlice` (relies on persistence invalidation as an implicit synthesis-staleness signal). Works today; brittle if the persistence→synthesis coupling is refactored.
- **INT-W2** — auto-update doesn't refresh the registry entry's file list/anchor/digest. Sticky reuse keeps working; edge case if the entire original file set renames across two consecutive auto-updates (could degrade to a weak match → block).

## Defect caught during execution
- **Phase 17 Nyquist validation** found a real substring-collision bug in `invalidatePersistenceEvidence` (`slice-1` keys falsely matched `slice-10`) — fixed with delimiter-aware matching.

## Open items for review
1. **INT-W1 / INT-W2** — confirm acceptable as-is, or address (small, scoped fixes).
2. Doc hygiene — reconcile REQUIREMENTS.md ledger + backfill SUMMARY.md (8 phases).

## Next step
Resolve INT-W1/W2 intent (accept as-is vs small fixes), then `/gsd-complete-milestone 1.6.0` (archive + tag). Reconciling the REQUIREMENTS ledger / backfilling SUMMARYs can be folded into the close or done first.
